<Window x:Class="EsemkaLaundryX.Views.JobView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EsemkaLaundryX.Views"
        mc:Ignorable="d"
        Title="JobView" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="gridJob" ItemsSource="{Binding Collection, Mode=TwoWay}" SelectedItem="{Binding Model}"  HorizontalAlignment="Left" Height="202" Margin="62,117,0,0" VerticalAlignment="Top" Width="368"/>
        <Button x:Name="btnRead" Content="Button" Command="{Binding ReadDataCommand}" HorizontalAlignment="Left" Margin="355,324,0,0" VerticalAlignment="Top" Width="75"/>
        <TextBox x:Name="txtName" HorizontalAlignment="Left" Height="23" Margin="534,210,0,0" TextWrapping="Wrap" Text="{Binding Model.Name, Mode=TwoWay}" VerticalAlignment="Top" Width="120"/>
        <Button x:Name="btnOpen" Content="Button" HorizontalAlignment="Left" Command="{Binding ExportExcelCommand}" Margin="62,324,0,0" VerticalAlignment="Top" Width="75" Click="BtnOpen_Click"/>

    </Grid>
</Window>
